node_modules
.idea
admin/i18n/flat.txt
admin/i18n/*/flat.txt
iob_npm.done
.DS_Store
#ignore .commitinfo created by ioBroker release script
.commitinfo

# TypeScript incremental info. The compiled adapter (build/) is committed
# so GitHub installs include the start file build/main.js.
*.tsbuildinfo

# vis-2 widget sources stay local; the copied bundle in widgets/sonos/ is committed
# so a GitHub install actually updates vis-2 (customWidgets.js).
src-widgets/node_modules/
src-widgets/build/
src-widgets/.__mf__temp/

# JsonConfig custom component and ioBroker.devices widgets: the sources are built with vite and
# the result is committed (admin/custom/, admin/dm-widgets/), because the module federation build
# takes minutes - see tasks.js
src-admin/node_modules/
src-admin/build/
src-admin/.__mf__temp/
src-devices/node_modules/
src-devices/build/
src-devices/.__mf__temp/
